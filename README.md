# نظام إدارة مركز الدهانات

نظام إدارة شامل لمراكز الدهانات يتضمن إدارة المبيعات، المخزون، العملاء، والتقارير.

## 🚀 الميزات

- ✅ إدارة المبيعات والفواتير
- ✅ تتبع المخزون والمنتجات
- ✅ إدارة العملاء والموردين
- ✅ نظام دفع مرن (كاش/آجل)
- ✅ تقارير مفصلة وإحصائيات
- ✅ واجهة عربية سهلة الاستخدام
- ✅ طباعة الفواتير
- ✅ نظام إشعارات متقدم

## 🛠️ التقنيات المستخدمة

- **Frontend**: Next.js 16, React 19, Tailwind CSS
- **Backend**: Next.js API Routes
- **Database**: MongoDB مع Mongoose
- **Authentication**: JWT
- **UI Components**: Lucide React Icons

## 📋 متطلبات النظام

- Node.js >= 18.0.0
- MongoDB >= 4.0
- npm >= 9.0.0

## ⚡ التثبيت والتشغيل

### 1. تحميل المشروع
```bash
git clone <repository-url>
cd paint-center-system
```

### 2. تثبيت التبعيات
```bash
npm install
```

### 3. إعداد قاعدة البيانات

#### خيار أ: MongoDB محلي
```bash
# تأكد من تشغيل MongoDB على المنفذ 27017
mongod
```

#### خيار ب: MongoDB Atlas (سحابي)
```bash
# احصل على connection string من MongoDB Atlas
# ثم حدث ملف .env.local
```

### 4. إعداد متغيرات البيئة
ملف `.env.local` جاهز ومُعد مسبقاً بالقيم الافتراضية:
```env
MONGODB_URI=mongodb://localhost:27017/paint_center_system
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
NEXTAUTH_SECRET=your-nextauth-secret-key
NEXTAUTH_URL=http://localhost:3001
```

### 5. تهيئة قاعدة البيانات بالبيانات التجريبية
```bash
node scripts/seed.js
```

سيتم إضافة:
- ✅ 2 مورد
- ✅ 19 منتج (دهانات، أدوات، مواد كيميائية)
- ✅ 8 عميل مع حدود ائتمانية
- ✅ 2 مستخدم (مدير ومستخدم عادي)

### 6. بيانات تسجيل الدخول التجريبية

**👑 المدير:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`
- البريد الإلكتروني: `admin@paintcenter.com`

**👤 المستخدم:**
- اسم المستخدم: `user`
- كلمة المرور: `user123`
- البريد الإلكتروني: `user@paintcenter.com`

### 7. تشغيل النظام
```bash
# للتطوير
npm run dev

# للإنتاج
npm run build
npm start
```

النظام سيعمل على: http://localhost:3001

## 👤 إنشاء مستخدم أول

1. اذهب إلى صفحة التسجيل: http://localhost:3001/login
2. اضغط "إنشاء حساب جديد"
3. أدخل بيانات المستخدم الأول

## 📊 هيكل قاعدة البيانات

### المجموعات الرئيسية:
- **users**: المستخدمين والصلاحيات
- **products**: المنتجات والأسعار
- **customers**: العملاء وحدود الائتمان
- **suppliers**: الموردين
- **sales**: المبيعات
- **invoices**: الفواتير
- **payments**: المدفوعات
- **stock**: حركة المخزون

## 🔧 الأوامر المفيدة

```bash
# تشغيل الخادم في وضع التطوير
npm run dev

# بناء المشروع للإنتاج
npm run build

# تشغيل الخادم في وضع الإنتاج
npm start

# فحص الكود
npm run lint

# تهيئة قاعدة البيانات (اختياري)
node scripts/seed.js
```

## 🌐 API Endpoints

### المصادقة
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/register` - إنشاء حساب جديد

### المنتجات
- `GET /api/products` - جلب جميع المنتجات
- `POST /api/products` - إضافة منتج جديد
- `PUT /api/products/[id]` - تحديث منتج
- `DELETE /api/products/[id]` - حذف منتج

### المبيعات
- `POST /api/sales` - إنشاء مبيعة جديدة
- `GET /api/sales` - جلب المبيعات
- `GET /api/sales/[id]` - تفاصيل مبيعة

### الفواتير
- `GET /api/invoices` - جلب الفواتير
- `GET /api/invoices/[id]` - تفاصيل فاتورة
- `POST /api/invoices/[id]/print` - تسجيل طباعة

## 🔒 الأمان

- تشفير كلمات المرور باستخدام bcrypt
- رموز JWT للمصادقة
- حماية من هجمات XSS و CSRF
- تحقق من صحة البيانات المدخلة

## 📱 الاستخدام

1. **إضافة المنتجات**: اذهب إلى قسم المنتجات وأضف منتجات الدهانات
2. **إضافة العملاء**: أضف عملاء مركز الدهانات مع حدود الائتمان
3. **البيع**: استخدم نقطة البيع لإنشاء فواتير سريعة
4. **التقارير**: راقب الأداء من خلال لوحة التحكم

## 🐛 الإبلاغ عن المشاكل

إذا واجهت أي مشاكل، يرجى فتح issue في المستودع مع:
- وصف المشكلة
- خطوات إعادة الإنتاج
- إصدار Node.js و MongoDB المستخدم

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT.

---

تم تطوير هذا النظام خصيصاً لإدارة مراكز الدهانات في مصر 🇪🇬